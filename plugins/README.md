# plugins

Repository plugins are data-only during discovery. `cc plugin run` is the only
runtime surface and revalidates the selected API 1 manifest and exact
entrypoint immediately before direct execution.
