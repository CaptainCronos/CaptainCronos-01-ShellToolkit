# Captain Cronos repository-owned health policy
#
# Schema: selector_type|selector|scope|unit|expectation|severity
# selector_type: role or profile
# scope: system or user
# expectation: present, active, enabled, absent
# severity: WARN or FAIL
#
# This catalog intentionally ships without enforcing policies. Host-specific
# expectations are not inferred from installed packages or capabilities.
